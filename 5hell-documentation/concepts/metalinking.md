# MetaLinking

## Meta Linking:

5hell allows you to use your home machine to load and scan meta objects.

To do this, 5hell loads a meta object of our home computer into a custom object(cob), named myx0.
Every subsequent shell you get is going to be myx0 +1
